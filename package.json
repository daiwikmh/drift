{
  "name": "drift",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @drift/web",
    "build": "npm run build --workspace @drift/web",
    "lint": "npm run lint --workspaces --if-present"
  }
}
